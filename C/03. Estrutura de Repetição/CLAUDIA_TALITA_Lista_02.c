// Modifique o programa anterior para exibir os números de 50 a 100.
#include <stdio.h>

int main()
{
    int limite, i;
    limite = 100;
    for (i = 50; i <= limite; i++)
        printf("%d\n", i);

    return 0;
}